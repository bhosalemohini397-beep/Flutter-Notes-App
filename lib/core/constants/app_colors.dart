import 'package:flutter/material.dart';

class AppColors {
  // Background (Paper-like)
  static const Color background = Color(0xFFFFF8E7); // Light cream

  // Primary (Highlight & Buttons)
  static const Color primary = Color(0xFFFFC107); // Amber

  // Secondary (Accent elements like FAB)
  static const Color secondary = Color(0xFFFFE082); // Soft amber

  // Text (Readable, warm dark brown)
  static const Color textPrimary = Color(0xFF5D4037); // Dark brown

  // Borders / Dividers / Subtle UI
  static const Color border = Color(0xFFD7CCC8); // Warm grey
}
